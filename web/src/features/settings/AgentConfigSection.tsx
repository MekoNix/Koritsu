/**
 * AgentConfigSection — «Конфигурация агентов» (`/settings/agent`).
 *
 * Один раздел вместо двух. Раньше умолчания агента и ключи поставщиков жили на
 * разных экранах, а настройка это одна: пресет, которым агент работает, и
 * ключ, которым за этот пресет платят, выбираются вместе. Выбрать пресет, не
 * видя, есть ли к нему ключ, значило переключаться между разделами туда и
 * обратно.
 *
 * Собственной разметки здесь нет намеренно — только порядок карточек. Сами
 * карточки остаются там же, где были (`AgentSection`, `ModelKeysSection`): у
 * каждой своя работа со службой, и слить их в один файл значило бы получить
 * экран, который делает четыре запроса и правит три разных ресурса.
 */
import { AgentSection } from './AgentSection'
import { ModelKeysSection } from './ModelKeysSection'

export function AgentConfigSection() {
  return (
    <>
      <AgentSection />
      <ModelKeysSection />
    </>
  )
}
