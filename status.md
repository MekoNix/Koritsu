# Status

Рабочие пометки: что делаем сейчас, что дальше, что отложено.
История выпущенного — в `CHANGELOG.md`.

## Сделано: 2.0.0a1 — uml_generator (закрыт 2026-08-27)

Цель: пересмотреть и улучшить генератор UML классов (перенесён из 1.x без изменений).

- [x] Разобрать текущий `extractor.py` / `builder.py` (2026-08-27) — итог ниже.

### Ошибки (неверный вывод)
- [x] C#: члены интерфейса помечаются `private` (в C# они неявно public).
- [x] C#: любое свойство печатается как `{ get; set; }` — хардкод; `{ get; }`, `{ get; private set; }`, `=>` теряются.
- [x] C++: `int a_, b_;` — берётся только первый declarator (`child_by_field_name("declarator")`), `b_` молча теряется.
- [x] C++: указатель на функцию `void (*cb)(int);` попадает в методы под именем `(*cb)`.
- [x] C++: массив `int arr[10]` → тип `int`, размер теряется.
- [x] builder `_bare_type`: для вложенных generics берётся самый внутренний (`shared_ptr<Base<T>>` → `T`, `List<Dictionary<K,V>>` → мусор) — связи уходят не к тому классу.
- [x] `_method_types`: `params int[] rest` → `paramsint`, `ct = default` — значения по умолчанию не отбрасываются.
- [x] objektis: динамические бэкенды (exec в subprocess, `dotnet run`) удалены — пользовательский код не выполняется (решение 2026-08-27, безопасность). Осталась IR + builder; фасад возвращает пустой граф с заметкой.
- [x] objektis: статические бэкенды Python (`py_static`), C# (`cs_static`, `Main()`), C++ (`cpp_static`, `main()`).

### Что теряется при извлечении
- [x] C#: `enum`, `record`, (`delegate` — не извлекается, редко нужен на диаграмме), вложенные типы (`class Nested` внутри класса) — не извлекаются вовсе (и как классы, и как цели связей: поле `Status St` не даёт ребра).
- [x] C#: событий, индексаторов, операторов, деструктора нет в методах/полях; `static`/`abstract`/`virtual`/`override`/`readonly`/`const` не отражаются (UML: static — подчёркивание, abstract — курсив).
- [x] C#: `internal protected` / `private protected` → просто `protected`; generic-параметры класса теряются (`Base<T>` → `Base`, `where` тоже).
- [x] C++: `template<class U> void tmpl(U)` внутри класса не распознаётся (`template_declaration` в теле не обрабатывается); `enum class`, `union`, вложенные `class Inner`, `using Ptr = …`, `friend` — пропускаются; `static`/`virtual`/`= 0`/`const`-методы/`override`/`final` не отражаются; деструктор помечен как конструктор; `const std::string&` в возвращаемом типе → `std::string`.
- [x] Связи: нет кратностей (сделано, кроме одного цвета для наследования/реализации — в раскладке) (`List<Shape>` → композиция без `*`, должна быть агрегация с `0..*`), нет ролей/подписей на рёбрах; `shared_ptr/unique_ptr` считаются композицией; наследование и реализация одним цветом.

### Builder / раскладка (по PNG `mixed`)
- [x] Палитра тёмной темы захардкожена; при экспорте на белом фоне стрелки `#cbd5e1` почти невидимы. Стили вынести в yaml, как `fragmos/styles.yaml`.
- [x] Уровни считаются только по наследованию: `Canvas`, зависящий от `Shape`, встаёт в нулевой ряд рядом с интерфейсами; внутри уровня дети не группируются под родителем.
- [x] Порт выбирается по расстоянию центров (`_edge_side`), поэтому потомок из соседней колонки входит в родителя сбоку, а не снизу; маршрут ложится через чужие блоки (Rect) и через всю диаграмму.
- [x] `MAX_PER_ROW=4`, `CHAR_W=7` — хардкод; ширина по числу символов без переноса.
- [x] Маршрутизация/порты (~150 строк) продублированы в `builder.py` и `objektis/builder.py` с косметическими расхождениями — вынести общий модуль до того, как разойдутся по смыслу.

### Тесты
- [x] `test_extractor.py` / `test_objektis.py` — самодельные `_assert` + `print`, `_skip_if_no_tsl` кидает RuntimeError вместо `pytest.skip`; C#-objektis тесты проверяют dotnet сами. Перевести на pytest-ассерты и `pytest.mark.skipif`; убрать `sys.path`-хак (есть `conftest.py`).
- [x] Тестов на разбор членов (generics, модификаторы, свойства, вложенные типы, множественные declarator'ы) нет — покрыть по образцам выше.

### Решения
- [x] Судьба 4 тестов «junction»: реализовано в раскладке, тесты зелёные. (Было: **рекомендация** — реализовать внутри переделки раскладки (layout → emit): по PNG главная визуальная проблема — каша из стрелок, общий «ствол» к родителю её снимает; 4 теста станут приёмочными. Если хотим a1 короче — убрать тесты.)
- [x] Раскладка диаграммы: размещение классов, стрелки наследования/композиции/зависимости без пересечений (переиспользовать подход fragmos: layout → emit).

- [x] Тесты: покрыть extractor и builder по образцам, как `tests/fragmos`.
- [x] Обновить `CHANGELOG.md` разделом `## 2.0.0a1`, коммит через `./commit.sh` (три коммита: extractor, раскладка, py_static).

## Сейчас: 2.0.0a2 — hokoku (отчёты по DOCX-шаблонам)

Решения (2026-08-27): свой движок на python-docx (не docxtpl); только движок, хранилище
отчётов/профилей и LLM-части — позже, отдельно; имя `hokoku`; префиксы типов тегов
(`global_` и т.п.) — в 1.5; логика в тегах (`|умолчание`, `#if`, `#each`) — в 1.5,
парсер их резервирует. Имя: tekton → hokoku (2026-08-27, по выбору пользователя). Разбор engrafo 1.x — в CHANGELOG-истории и `git log`.

- [x] Модель значений, теги, walker по всему документу, lossless-подстановка, markdown,
      списки/ссылки/стили, картинки с нарезкой, таблицы, safety, PDF, тесты, лаборатория.
- [x] LibreOffice установлен (2026-08-27), отчёт проверен по PDF: титульник, списки, стили, таблицы, ссылки, схема по листам, диаграмма классов, колонтитулы — верно.
- [x] Формулы (OMML) и оглавление (TOC) — сделано 2026-08-27; для превью в LibreOffice нужен `libreoffice-math`.
- [ ] Сквозной шаблон `Report(sections=[...])` — по запросу.
- [ ] Стили заголовков/кода: если в шаблоне нет `Heading N`/`Code`/`Caption`/`Quote` —
      свои запасные; проверить на реальном титульнике кафедры (образцов нет — нужны ваши).
- [x] Картинки/таблицы/блоки в колонтитулах, ячейках, текстовых полях — покрыто матричным тестом `test_templates.py`.
- [ ] Обновить `CHANGELOG.md`, коммит `./commit.sh`.

### Что улучшить в hokoku (разбор 2026-08-27, по пробам краевых случаев)

**Дефекты (проверено)**
- [x] Тег внутри гиперссылки (`w:hyperlink`) и внутри поля (`w:fldSimple`) невидим: `para.runs`
      отдаёт только прямые `w:r`. `extract_tags` его не находит, `render` не подставляет.
      Лечится обходом всех `w:r`-потомков абзаца (кроме `w:del`).
- [x] Числа/bool в значениях → `HokokuError: неподдерживаемый тип int`. Приводить к строке.
- [x] `render(..., output=BytesIO)` не работает — только путь. Принимать file-like и `None` → bytes
      (нужно будущему API: отдать файл без записи на диск).
- [x] SVG/EMF-картинки не читаются (PIL). Либо честная ошибка с подсказкой, либо опциональная
      конвертация SVG → PNG (cairosvg) — drawio отдаёт и SVG.
- [x] Ключи тегов не нормализуются по Unicode (NFC/NFD: `й`, `ё` с macOS) — `{{фио}}` из Word
      и ключ из JSON могут не совпасть. Нормализовать NFC на обеих сторонах.

**Markdown**
- [x] Списки задач `- [ ]` / `- [x]` → сейчас текст «[ ] задача»; рисовать ☐/☑.
- [x] Вложенные цитаты `> >` → «> вложенная».
- [x] Подсветка синтаксиса в `Code` (pygments, опционально) и нумерация строк — в методичках
      часто требуют; сейчас всё одним шрифтом.
- [x] Картинка с размером `![подпись](f.png){width=10cm}` и выравниванием.
- [x] Значение, содержащее `{{…}}`, вставляется как есть (не раскрывается) — это правильно
      (иначе инъекция), зафиксировать тестом.

**Оформление / настройки**
- [x] `styles.yaml` как у fragmos/uml: шаблоны подписей (`Рисунок {n} — {caption}` /
      ГОСТ 7.32 `Рисунок {n} – {caption}`), шрифт и размер кода, заливка шапки таблицы,
      ширина картинок по умолчанию, отступы; сейчас часть констант в `docx_ops.py`.
- [x] Абзац перед картинкой («Блок-схема алгоритма:») не «прилипает» к ней — ставить `keepNext`
      на абзац с тегом, если первый блок значения — картинка.
- [x] Ссылки на рисунки/таблицы в тексте («см. рисунок {ref:схема}») — нумерация есть,
      ссылок нет.
- [x] Вставка draw.io XML напрямую (`Image` из XML → drawio CLI внутри hokoku), чтобы fragmos/uml
      отдавали XML, а не PNG; лаборатория уже делает это снаружи.
- [x] `Image.source` — абсолютный путь не ограничен каталогом (в отличие от `![..](f)` в
      markdown). Для API нужен режим «только внутри images_dir».

**API / инфраструктура**
- [x] `extract_tags` отдаёт только первое место тега; для UI полезны все места и число
      вхождений (в какой секции/колонтитуле).
- [x] `render(Document)` меняет переданный объект на месте — задокументировать или копировать.
- [x] Производительность: 300 markdown-абзацев со списками — 0.8 с; по `abstractNum` на каждый
      список раздувает numbering.xml. Один `abstractNum` на вид списка + `w:num` с
      `lvlOverride/startOverride`.
- [ ] Сноски: теги там не ищутся (решено), блоки не поддерживаются — оставить.


## Дальше

- Лаборатория uml: `~/koritsu2-extras/labs/uml` (`2_run.py`, `3_serve.py --port 8766`) — образцы py/cs/cpp × классы/объекты × dark/light.

- objektis: трассировать `if`/`for` хотя бы для простых случаев (сейчас — заметка и пропуск).
- objektis: C# `delegate`/события и C++ `std::map` как коллекции слотов.



- Вернуть лабораторию (`~/koritsu2-extras/labs`) в репозиторий, когда решим, где ей жить.

## Ревью 2026-08-27 (два агента, по 12 мин; дефекты подтверждены по PNG)

### hokoku — дефекты
- [ ] Оглавление в PDF пустое: LibreOffice не обновляет поле TOC при конвертации (SEQ считает).
      Нужно обновление полей через UNO (`updateAll`) в `pdf.py` или своя сборка TOC по заголовкам.
- [ ] `Image(caption=None)` всё равно печатает «Рисунок N» — нет способа вставить картинку без
      подписи (лого в колонтитуле стало «Рисунок 1»). Добавить `caption=False`; в колонтитулах
      не нумеровать.
- [ ] `{ref:имя}` в статическом тексте шаблона не обрабатывается и не попадает в `unresolved_refs`.
- [ ] Ширины колонок: колонка с «a» получает ~60 %, длинный текст рвётся; шапка переносится при
      свободном месте. Считать по максимуму содержимого с приоритетом длинных колонок.
- [ ] Картинка в ячейке без `w:tcW` (типовой титульник) рисуется по ширине страницы и вылезает.
      Брать ширину из `w:tblGrid`/`gridCol` или число колонок.
- [ ] Вложенная цитата `> >` — без доп. отступа (помечено сделанным, но визуально не видно).
- [ ] Таблица внутри пункта списка теряет отступ уровня.
- [ ] README: эмодзи в PDF — тофу (шрифт LibreOffice), в Word нормально.

### hokoku — добавить
- [ ] `Code(caption=, ref=)` → «Листинг N — …» (SEQ, ссылки) — S.
- [ ] `caption=False` для картинок/таблиц; запрет нумерации в колонтитулах — S.
- [ ] Обновление полей при PDF-конвертации (UNO `updateAll`) — M.
- [ ] `{ref:}` в тексте шаблона + `unresolved_refs` — S.
- [ ] `check_template()`: теги без значений, значения без тегов, нет стилей Heading/Caption — S.
- [ ] Широкие таблицы: `font_size`, альбомная секция для >8 колонок — M.
- [ ] Приложения по ГОСТ 7.32: `Appendix("А", …)`, нумерация «А.1» — M.
- [ ] Библиография `{cite:…}` → список источников + «[3]» — M.
- [ ] Архитектура: собрать подписи/нумерацию в один `captions.py` (единый счётчик, правило
      «часть документа → нумеровать/нет») до «Листинг N» и приложений.

### fragmos — дефекты (схема молча врёт)
- [ ] C#: `goto case` выбрасывается; C++: fall-through в `switch` теряется (после case 1 — слияние).
- [ ] `throw` не рисуется; тело `try { throw 1; }` исчезает.
- [ ] Python `for … else`: тело `else` попадает внутрь цикла.
- [ ] C++ range-for подписан сырым «auto x : v»; в подписи теряется `<int>`.
- [ ] Метки `case` — прямоугольниками («процесс»), default подписан `_` во всех языках.
- [ ] `break` и `continue` из одной ветки — на одной вертикали (нечитаемо); наконечников нет (п.4.2.1).

### fragmos — добавить
- [ ] Наконечники на линиях не сверху-вниз/слева-направо (п.4.2.1) — S.
- [ ] Раздельные коридоры для `break`/`continue`/`return` — M.
- [ ] Честный fall-through / `goto case`; `throw` блоком или явное предупреждение — M.
- [ ] Метки case на линиях от «решения», «иначе» вместо `_` — S.
- [ ] `return_shape: terminator` («Возврат X») — S. `size_mode: uniform` + перенос подписей — M.
- [ ] Канал диагностики `manifest.warnings` («в схему не вошло: goto case») — hokoku покажет.
- [ ] Архитектура: подписи узлов размазаны по трём `*_ast.py` — единый слой «узел → подпись».

### uml_generator — дефекты / добавить
- [ ] Одноимённые классы (`partial`, разные namespace) затирают друг друга — раскладка ключуется
      по имени (`builder.py`); перейти на стабильный id, `partial` склеивать — M.
- [ ] `const` приклеен к типу возврата (`T& const`); текст вылезает за рамку при длинных типах.
- [ ] Множественное наследование `public A, protected B` — разные наконечники, второе ребро сбоку.
- [ ] Сокращение длинных типов (`System.Collections.Generic.List<T>` → `List<T>`) — S;
      UML-нотация параметров `v: const T&`, `{query}` вместо `const` — опцией — M.

## fragmos — запрошено (2026-08-27)

- [ ] **Разбивка блок-схемы на страницы силами fragmos**, а не растровой эвристикой hokoku.
      `generate_from_code(..., page_height_cm=…)` (или параметр в cfg) → многостраничная
      draw.io-схема: разрез только в зазоре между блоками; на месте разрыва — настоящий
      символ «Соединитель» ГОСТ 19.701-90 (кружок с номером) внизу листа и вверху следующего,
      на оси каждой пересекаемой линии (в т.ч. на ветке «Нет», если разрез попал на две
      параллельные линии). hokoku тогда вставляет N картинок как «Рисунок 1 (лист k)»;
      пиксельная нарезка `split_pages` остаётся запасным вариантом для чужих картинок.
      Дополнительно: fragmos отдаёт `page_breaks` (список y между блоками) в manifest — для тех,
      кто рендерит сам.

## Отложено / открытые вопросы (fragmos)

- `try/except` рисуется ромбом — ГОСТ символа для исключений не даёт.
- `return` в gost — параллелограмм; альтернатива по стандарту — терминатор «Возврат X» (`return_shape: terminator`, не реализован).
- Стрелки на линиях справа налево (стык `return`, слияние ветки «Да») — по п.4.2.1 нужны; решить: наконечники везде или только на нестандартных направлениях.
- Одинаковый размер символов одного типа (п.4.1.3) — опция `size_mode: uniform`.
- `break` и `continue` в одной ветке одного цикла могут пересечься.
