# Koritsu — зависимости пакетов (packages/*).
drawpyo>=0.2.5
python-docx>=1.1
Pillow>=10
pymupdf>=1.26          # разбор PDF в materials: текст с номерами страниц, встроенные картинки
openpyxl>=3.1          # разбор таблиц Excel в materials: значения листов текстом
cairosvg>=2.7
matplotlib>=3.8        # графики в zuhyo: рисуются бэкендом Agg, без экрана и без окна
pygments>=2.17
PyYAML>=6.0
httpx>=0.27            # транспорт слоя моделей (без SDK поставщиков)
tree-sitter>=0.23
tree-sitter-python>=0.23
tree-sitter-cpp>=0.23
tree-sitter-c-sharp>=0.23

# Служба api (HTTP поверх orchestrator).
fastapi>=0.115         # OpenAPI строится сам, из него — клиент сайта
uvicorn[standard]>=0.30  # сервер; [standard] — быстрый цикл событий и разбор HTTP
sqlalchemy>=2.0
alembic>=1.13          # миграции с первого дня: без них новое поле = «удалить базу»
argon2-cffi>=23.1      # пароли; bcrypt отвергнут: у него молчаливый предел в 72 байта
python-multipart>=0.0.9  # загрузка материалов телом запроса
cryptography>=42       # ключи моделей шифруются на диске секретом сервера
# Клиентская половина моста распознавания рукописи. Пакет приезжает и с
# uvicorn[standard], но там он транспорт сервера, а здесь — прямой импорт
# нашего кода: строка стоит затем, чтобы смена набора uvicorn не унесла мост.
websockets>=13

# Тесты (нужен ещё drawio CLI + xvfb-run для PNG).
pytest>=8
