"""
kyotsu (共通, «общее») — то, что нужно нескольким пакетам сразу.

Правило разреза: `hokoku`, `llm`, `materials`, `fragmos` и `uml_generator` друг
о друге не знают, соседей импортирует только `orchestrator`. Из-за этого одно и
то же приходилось держать копиями: загрузчик tree-sitter лежал байт-в-байт
двумя файлами, таблица ширин символов — тремя. Копия расходится молча, и узнать
о расхождении можно только по кривой картинке.

`kyotsu` — исключение, но узкое и проверяемое: сюда кладётся только то, что не
знает ни о задаче, ни о соседях. **Ничего из проекта этот пакет не импортирует**
(проверяется `tests/kyotsu/test_border.py`), поэтому зависеть от него может кто
угодно, не втягивая за собой остальное дерево.

Чего сюда класть нельзя: всё, у чего есть мнение о предметной области — модели
данных, форматы, настройки. Общий пакет, куда складывают «просто общее»,
через полгода становится вторым центром проекта.

`Notice` (2.0.0a4.2) — единственная модель данных, которая сюда пущена, и пущена
по тому же правилу: у неё нет мнения о предметной области. Она знает, что у
замечания есть источник, уровень, код и текст, и не знает ни одного кода —
коды остаются у пакетов, которые их выдают. Держать её в общем месте пришлось
потому, что форма замечания и есть то, о чём три пакета обязаны договориться:
разъехавшись, она разъезжается молча — и в интерфейсе появляется третий разбор
чужих словарей.
"""

from .notice import LEVELS, Notice
from .text import EM, char_em, text_width
from .ts import get_language, get_parser

__all__ = ["EM", "char_em", "text_width", "get_language", "get_parser",
           "Notice", "LEVELS"]
