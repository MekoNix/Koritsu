"""
config.py — Конфигурация по умолчанию для рендерера flowchart.

Любой ключ можно перегрузить через `cfg_overrides` в generate_from_code.
"""

DEFAULT_CFG = {
    # ── Страница ───────────────────────────────────────────────────────
    "page_center_x":       500,  # ось X главной ветки
    "page_top_y":          20,   # верх первого блока
    "gap_y":               40,   # вертикальный зазор между элементами

    # ── Размер блока от текста ─────────────────────────────────────────
    # "text"   — ширина/высота считаются из реальной ширины текста
    #            (таблица ширин символов Helvetica) и переноса по словам
    # "legacy" — старая ступенька: ×(len//50 + 1)
    "size_mode":           "text",
    "font_size":           12,   # px, шрифт draw.io по умолчанию
    "line_h":              16,   # высота строки, px
    "max_text_w":          260,  # px: перенос по словам при превышении
    "pad_x":               16,   # горизонтальный отступ текста от края
    "pad_y":               10,   # вертикальный отступ
    "size_grid":           10,   # округление размеров вверх до сетки

    # ── IF ─────────────────────────────────────────────────────────────
    "if_branch_gap":        20,  # горизонт. расстояние от края ромба до центра ветки
    "if_branch_vgap":       15,  # вертикальный зазор: низ ромба → верх первого блока ветки
    "if_branch_min_gap":    40,  # мин. зазор между bbox-ами веток (не между центрами!)
    "if_empty_branch_half": 60,  # полуширина, резервируемая под пустую ветку

    # ── SWITCH ─────────────────────────────────────────────────────────
    "switch_case_gap":      20,  # зазор между колонками case
    "switch_case_min_half": 60,  # минимальная полуширина колонки

    # ── WHILE / FOR — коридоры ─────────────────────────────────────────
    "while_corridor_base":  80,  # базовый коридор на глубине 0
    "while_corridor_step":  20,  # уменьшение коридора на каждый уровень вложенности
    "while_corridor_min":   30,  # минимальная ширина коридора

    # ── WHILE / FOR — возвратная стрелка ───────────────────────────────
    "while_back_turn_gap":  20,  # ниже последнего блока тела — поворот влево
    # Высота, на которой back-arrow поворачивает справа налево над rh.
    # Также позиция arrowhead'а: он садится на вертикальную стрелку,
    # приходящую в rh сверху, на этом смещении над верхней границей rh.
    "while_back_top_gap":   18,

    # ── Подписи Да / Нет ───────────────────────────────────────────────
    # Текст берётся из стиля (styles.yaml → labels.yes / labels.no);
    # parser кладёт его сюда. Значения ниже — запасные.
    "label_yes":            "Да",
    "label_no":             "Нет",
    "label_w":              44,
    "label_h":              20,
    "label_gap":            4,   # отступ подписи от угла фигуры
    "label_dy":             18,  # подъём подписи над серединой ромба

    # ── Отладка ────────────────────────────────────────────────────────
    "show_bbox":            False,  # рисовать bounding box веток/циклов
    "bbox_pad_x":           10,
    "bbox_pad_y":           6,
}
