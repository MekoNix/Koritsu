"""
runs — прогоны модели как задания очереди: ключ, лимит, обработчики.

Три части, и разрез между ними такой:

    model.py      где ключ поставщика на минуту становится текстом
    limits.py     цены наших видов работ, месячный потолок и расход человека
    handlers/     шесть обработчиков — по модулю на вид задания
    routes.py     `GET /api/usage`

Пакет ничего не знает про HTTP, кроме одного маршрута расхода: прогон ставится
заданием (`POST /api/jobs`), а исполняется воркером в другом процессе. Отсюда и
правило: всё, что здесь написано, обязано работать без `Request` — обработчик
получает `JobContext` и больше ничего.
"""
from __future__ import annotations

from .limits import LIMIT_EXHAUSTED, Расход, расход
from .model import NO_KEY

__all__ = ["Расход", "расход", "LIMIT_EXHAUSTED", "NO_KEY"]
