"""
notifications — колокольчик: что случилось, пока человека не было.

**Писем о готовности нет.** Значит единственный канал —
интерфейс, и у него две половины: живая (поток событий `api/events`, пока
вкладка открыта) и запомненная (эта таблица, для всех остальных случаев).
Половины не заменяют друг друга: поток не переживает закрытую вкладку, а список
не показывает происходящее сейчас.

Строка бывает не только вестью: приглашение в рабочее пространство приходит
сюда же и несёт две кнопки — «принять» и «отклонить». Отдельного списка
приглашений нет и не нужно: приглашение — ровно то, что случилось, пока человека
не было, а таблица для такого уже есть.

Наружу отсюда — `Notification` (модель, чтобы `api.models` знал про таблицу),
`service` (кто заводит и кто читает) и `routes` (маршруты колокольчика).
"""
from __future__ import annotations

from .models import Notification
from .service import WORKSPACE_INVITE, пригласили, при_завершении

__all__ = ["Notification", "при_завершении", "пригласили", "WORKSPACE_INVITE"]
