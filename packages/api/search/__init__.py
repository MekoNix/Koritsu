"""
search — один маршрут: найти работу или файл по куску имени.

Своим подпакетом, а не полем `q` у `GET /api/projects`, потому что спрашивают здесь
другое. Список проектов отвечает на вопрос «что лежит в этом пространстве»;
поиск — на «где у меня то, что называется так», и отвечает он не только
работами, но и файлами внутри них. Пространство обязательно у обоих: искать по
всем сразу значило бы показывать в одном пространстве имена работ из другого.

Своих таблиц нет: ищется по проектам и по материалам, которые лежат
на томе, а не в базе. Отсюда и разрез внутри — `service` знает, что такое
совпадение и где искать, `routes` не знает ничего, кроме формы ответа.
"""
from __future__ import annotations

from .routes import router

__all__ = ["router"]
