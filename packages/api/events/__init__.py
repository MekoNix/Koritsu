"""
events — потоки `text/event-stream`: как служба рассказывает о происходящем.

Два модуля и один разрез: `sse.py` знает формат кадра и правило «сессия на
каждый опрос», `routes.py` — что именно качать. Формат отдельно, потому что
читателей у него двое (поток задания и поток человека), а кадр, собранный по
месту, однажды окажется собран с переносом строки внутри `data` — и клиент
получит обрывки вместо событий.

Опросом базы, а не подпиской. Слушать было бы не у кого: события пишет **другой
процесс** (воркер), а SQLite межпроцессных уведомлений не умеет. Полсекунды
задержки (`KORITSU_SSE_POLL_S`) человек не замечает, а очередь на этом не
усложняется ни строкой — что и решает, потому что второй канал между воркером и
веб-процессом пришлось бы держать живым, а он однажды не переживёт выката.
"""
from __future__ import annotations

from .routes import job_router, user_router

__all__ = ["job_router", "user_router"]
