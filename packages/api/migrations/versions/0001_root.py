"""root — корень цепочки миграций. Пустая намеренно.

Revision ID: 0001_root
Revises:
Create Date: 2026-09-03

Таблиц здесь нет: каркас службы их не заводит, их заводят подпакеты — аккаунты
и сессии, workspace с проектами и корзиной, материалы и ключи. Пустая
миграция — не заготовка, а якорь.

Зачем якорь. Первые миграции пишутся параллельно и создаются в один и тот же
час. Без корня у каждой получился бы `down_revision = None`, то есть три
независимых корня — а `alembic merge` сводит ветви, растущие из общего предка, и
три корня не сведёт вовсе. С якорем у всех трёх `down_revision = "0001_root"`,
и это три обычные головы, которые сводит одна команда

    alembic merge -m "a5" <head> <head> <head>

Второе, что даёт пустая миграция: таблица `alembic_version` появляется на самом
первом `upgrade head`, ещё до первой настоящей таблицы. Значит, на любой базе,
которую вообще трогала служба, версия схемы известна — и не бывает состояния
«таблицы есть, а версии нет», из которого Alembic выбирается только руками.
"""
from __future__ import annotations

revision = "0001_root"
down_revision = None
branch_labels = None
depends_on = None


def upgrade() -> None:
    pass


def downgrade() -> None:
    pass
