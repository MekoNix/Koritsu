"""
export — выгрузка файлов пользователя из проекта одним zip.

Не проект целиком, а только файлы пользователя: архив материалов и артефактов;
код проекта — в git. Отсюда содержимое архива и
то, чего в нём нет: ни базы, ни настроек службы, ни журналов прогонов, ни
внутренней раскладки каталога — всё это наше, а не человека.

    Что где
    -------

    handlers.py   задание `export`: собрать архив и положить артефактом
    routes.py     `POST /api/projects/{id}/export` → 202 с карточкой задания

Скачивания здесь нет намеренно: архив — обычный артефакт проекта, и забирают
его тем же `GET /api/projects/{id}/artifacts/{aid}`, что и схему, и собранный
отчёт (`modules/artifacts.py` объясняет, почему маршрут один на всех). Тип он
получит по содержимому — `application/zip`, а не `docx`: различать их по первым
байтам нельзя, оба zip, и опись внутри архива — единственное, что их отличает.
"""
from __future__ import annotations

from .handlers import ARCHIVE_NAME, EXPORT_TOO_LARGE, собрать_архив

# `routes` отсюда **не** импортируется, и это не забывчивость. Пакет тянет к
# себе `registry.load_handlers()` — то есть и подпроцесс задания, где FastAPI не
# нужен вовсе; строка `from .routes import router` означала бы импорт всего
# веб-каркаса в каждом подпроцессе. Роутер берёт `api/routes.py` по полному
# имени, как и у соседей.

__all__ = ["собрать_архив", "ARCHIVE_NAME", "EXPORT_TOO_LARGE"]
