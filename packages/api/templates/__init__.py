"""
templates — шаблоны отчётов человека: DOCX, живущий вне проектов.

Раздел настроек «Шаблоны отчётов» делается вместе с API. До этого шаблон
существовал только внутри работы: `POST /api/projects`
принимал DOCX телом запроса, клал его артефактом проекта и забывал; человек,
заводящий пятую работу по тому же ГОСТу, искал один и тот же файл в пятый раз.

    таблица  templates       id, user_id, name, bytes, tags, sha256, даты
    том      {data}/users/<uid>/templates/<template_id>.docx
    маршруты /api/templates  список, загрузить, удалить, скачать

Три решения, ради которых подпакет вообще заведён, и все три — про границы.

**Байты на томе, строка в базе.** Список шаблонов читается на каждом открытии
настроек и при каждом создании работы; возить через сессию мегабайты DOCX ради
трёх колонок — это то же самое, чего служба не делает и с материалами.

**Теги считает оркестратор** (`orchestrator.template_tags`) — та же функция,
которой строится манифест при создании проекта. Свой разбор DOCX здесь
разошёлся бы с манифестом молча, и человек видел бы в списке «12 тегов», а в
заведённой по нему работе — девять.

**Квота общая с проектами.** Шаблоны лежат в каталоге человека и считаются
`projects.service.bytes_used` наравне с работами: отдельный порог на файлы вне
проектов означал бы, что «250 МБ на человека» больше ничего не значит.

Маршруты отсюда не выставляются: их берёт `routes.collect()` прямо из
`templates.routes`. Импорт роутера здесь замкнул бы круг — `api.models` тянет
`templates.models`, то есть этот файл, а роутер тянет зависимости доступа,
которые тянут `api.models` обратно (тот же довод, что у `tokens`).
"""
from __future__ import annotations

from .models import NAME_LEN, ReportTemplate

__all__ = ["ReportTemplate", "NAME_LEN"]
