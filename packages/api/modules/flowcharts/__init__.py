"""
flowcharts — модуль блок-схем: чужой исходник → draw.io XML.

Экран владельца («Блок-схемы»): слева код и параметры — язык и вид генерации,
справа схема во встроенном draw.io, кнопки «скачать XML», «скачать Word»,
«открыть в draw.io». Отсюда и три маршрута: перечень режимов для левой половины,
предпросмотр для правой (без проекта и без записи) и построение в проект — то,
что потом попадает в отчёт и скачивается. «Скачать Word» модулем не делается:
это отчёт, то есть `hokoku` через задание сборки; «открыть в draw.io» сайт
делает сам по XML, службе там делать нечего.

Что модуль отдаёт наружу пакету — `MODULE` и `router`, ровно то, что просит
реестр (`api/modules/__init__.py`). Ни одного другого имени: всё остальное здесь
подробность, а подробность, названная в реестре, становится договором.
"""
from __future__ import annotations

from .. import ModuleInfo
from .routes import router

MODULE = ModuleInfo(id="flowcharts", title="Flowcharts", ready=True,
                    routes_prefix="/api/flowcharts")

__all__ = ["MODULE", "router"]
