"""
kadai — модуль заданий: одна задача условием на входе, готовая работа на выходе.

Сам сценарий живёт в пакете `kadai` и зовётся заданиями очереди (`kadai_run`,
`kadai_rework`); маршрутов ему для этого не нужно вовсе. Карточка и два
маршрута здесь нужны сайту, и ровно по двум причинам, каждая — про то, чего у
службы до сих пор не было:

1. **Условие**. Прогон отказывает («условие задачи не приложено»), пока материал
   не назван условием, а назвать его умеет только `Project.set_condition`. Без
   маршрута страница заданий не запускается ни разу.
2. **Ход работы**. Стадии, остановка с вопросом, распознанное условие и
   собранные файлы лежат на томе (`kadai.status`), а не в карточке задания:
   считает их один процесс, показывает другой. `job.result` отдаёт срез
   последнего прогона и **имена** готовых файлов, а сайту нужны идентификаторы
   артефактов — иначе показать PDF и дать скачать DOCX нечем.

Карточка модуля обязательна и сама по себе: сайдбар строится по
`GET /api/modules`, и модуля, которого там нет, на сайте нет вовсе (правило
брифа). `routes_prefix` — `/api/projects`, как у `reports`: работа живёт в
проекте, своего корня у модуля нет.
"""
from __future__ import annotations

from .. import ModuleInfo
from .routes import router

MODULE = ModuleInfo(id="kadai", title="Assignments", ready=True,
                    routes_prefix="/api/projects")

__all__ = ["MODULE", "router"]
